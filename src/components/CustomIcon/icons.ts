import { ICON_DAY_NIGHT } from './constants';
import { createCustomIcon } from './utils';

/** 日夜示例图标，用法与 antd icon 一致：`<DayNightIcon style={{ fontSize: 16 }} />` */
export const DayNightIcon = createCustomIcon(ICON_DAY_NIGHT);
