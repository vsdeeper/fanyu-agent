/** 加载失败时面板内提示（与 FileCard 缺地址提示区分：此处已打开面板） */
export const FILE_PREVIEW_LOAD_ERROR = '无法加载文件预览';

/** 按扩展名视为 Markdown 渲染 */
export const MARKDOWN_EXTENSIONS = new Set(['md', 'markdown', 'mdx']);

/** 不含参数的 Markdown MIME */
export const MARKDOWN_MEDIA_TYPES = new Set(['text/markdown', 'text/x-markdown']);
