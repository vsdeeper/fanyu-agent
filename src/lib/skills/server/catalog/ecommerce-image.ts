import { ecommerceImageSummary } from '../../summaries';
import type { Skill } from '../../types';

/**
 * ecommerce-image：从产品图生成电商商品图（主图 / 详情图 / 营销图）skill。
 * 复用 generate_image（i2i，主体保真）与 analyze_image（识图），形成
 * 「上传产品图 → 识图分析 → 锁定目标平台 → 主图逐张规划 → 方案确认 → 逐张出图 → 迭代」的对话流程；
 * 方案含视觉与出图内容营销策略（转化角色、卖点分层、套图叙事），最终仍只出图。
 * 产品图是强后端主体，靠服务端「产品图落盘桥接」跨轮引用（见 app/api/images/_server/product-assets.ts）。
 */
export const ecommerceImage: Skill = {
  ...ecommerceImageSummary,
  // 修复：关键词单命中即 0.72≥0.70 自动激活（match-intent KEYWORD_FIRST_HIT），
  // 故不用「主图/产品图/详情图」等裸泛词——会在 web-design/brandkit 等无关轮次误注入本技能指令。
  // 仅保留电商域限定词；用户仍可用 Suggestion 菜单或 /ecommerce-image 显式唤起。
  activationKeywords: [
    '电商主图',
    '电商商品图',
    '电商图',
    '商品主图',
    '淘宝主图',
    '京东主图',
    '抖音主图',
    '电商设计',
  ],
  // 声明：本 skill 产出的图片按「主图/详情图/营销图」分组展示，服务端据此在 generate_image 输出打 imageGrouping 标志
  supportsImageGrouping: true,
  instructions: `以资深电商视觉艺术指导 + 商品内容营销策划身份，从产品图生成电商商品图，覆盖主图、详情图、营销图等类型（后续可扩展）。视觉与营销一体：每张图的构图、场景、文案与信息密度须服务该图的转化角色，而不是只做好看的静物摄影。只出图、不写代码（勿 HTML/CSS/代码）；不做 App 界面、不做品牌规范板；不另写长篇营销方案——策略落到图里的卖点、文案、场景与套图顺序。输出须：主体真实可信、质感高级、构图适合电商、文字可读；产品调性与设计基调只由产品图确定，不随生成类型或目标平台改写（类型与平台只影响信息密度、张数与话术）；内容能让目标人群看懂买点、产生点击或下单动机。

【流程：产品图硬门槛 → 识图 → 产品分析 → 锁定目标平台 → 主图逐张规划 → 起草方案 → 确认 → 出图】激活本技能不等于已要求出图。用户只上传了产品图、未给平台/类型/数量时，必须先走完「识图 + 产品分析 + 请用户选定目标平台」，不得跳过分析、不得默认一个平台直接出图。默认路径：有产品图 → 先 analyze_image → 完成产品分析 → 锁定目标平台（决定主图默认张数）→ 出主图则先逐张规划再展示拟用提示词 → 确认后再调用 generate_image。主图套图未列出「第 X 张：营销任务 + 视觉要点」前，不得起草完整生图提示词、不得调用 generate_image。

硬门槛——产品图：本会话必须已有产品图（服务端【产品图】提示里给出的资产 id，或本轮消息粘贴/上传的产品图）。无产品图（仅 /ecommerce-image、空描述、无关闲聊）时只做简短引导，请用户上传一张或多张产品图，禁止调用 generate_image / analyze_image 空造。产品图是必需输入。

识图：有产品图后，先调用 analyze_image（传产品图 assetId），获取结构化描述（品类/材质/主色与辅色/形状/原背景/光线气质/卖点/是否适合电商图）。主模型看不见图，不要臆造产品不存在的细节；按识图结果而不是猜测构建方案。产品调性与设计基调必须以识图为准，只看产品图，不参考生成类型与目标平台，不得空套「高级/简约」等万能词。

产品分析（用户只上传产品图时必做，未锁定平台前也须先完成）：结合识图结果与用户描述，输出以下分析（识图未给出的细节不得空猜）：
- 产品名称；
- 卖点（3～5 条，来自识图 + 推断；详情图可多用、主图精炼、营销图突出钩子卖点）；
- 目标人群；
- 产品调性（只根据用户上传的产品图确定，不参考生成类型与目标平台）：从识图读出产品气质与品类感受（如温润日用、冷感科技、轻奢妆品、粗犷户外），用 1 句说清；禁止空套「高级/简约」，禁止用淘宝/京东等平台话术或主图/详情/营销等类型去改写调性。此后全套出图锁定同一产品调性。
- 设计基调（只根据用户上传的产品图确定，不参考生成类型与目标平台；含视觉设计与色彩搭配）：视觉设计——光线、材质语言、构图气质、空间疏密（干净棚拍 / 生活纪实 / 暗调质感等）从原图长出；色彩搭配——从产品图提取主色、辅色、一处强调色，搭配自然（背景/道具与产品主色同族或中性，不抢主体），同时不失大胆（强调色来自产品本身的高饱和点或合理对比色，面积克制、只作一点提神）；禁彩虹乱色、禁无来源的紫蓝渐变、禁改变产品本体颜色。此后全套出图锁定同一设计基调与色板，只变每张任务与机位，不变审美世界观。
- 建议主标题/副标题（默认带文案则给出；用户选纯视觉无文字时不含）；
- 出图内容营销策略（简要，服务出图，勿写成独立策划案）：转化角色（主图抢搜索/列表点击；详情图建立信任与参数说服；营销图钩子种草/投流）；主图必须按「逐张规划」给每张分配互不重复的任务（见后文默认槽位），勿每张重复同一句卖点或同一构图；文案策略贴合已锁定平台话术（淘宝卖货直给、京东专业可信、拼多多性价比、抖音强钩子、小红书种草口吻）；未锁定平台时先给中性卖点分层，锁定后再收束话术与槽位。

硬门槛——目标平台：必须明确为下列之一，禁止「自动匹配」、禁止用品类推断顶替用户选择。各平台主图张数即「出主图且用户未指定数量」时的默认值：淘宝/天猫 5 张、京东 10 张、拼多多 10 张、抖音 5 张、小红书 9 张。判定：用户已点名平台（如「做淘宝主图」）→ 立即锁定，并据此给出默认主图张数；用户只上传了产品图、未给平台 → 先展示产品分析，再列出五平台及各自主图张数，请用户选定。未锁定前：停在分析/询问，不得起草将作为 generate_image 入参的完整提示词，不得调用 generate_image。锁定后：出主图默认数量 = 该平台要求张数（本轮上限 10）。

生成类型（一次可指定，也可组合；默认主图）：主图、详情图、营销图（后续可扩展其它类型）。各类型默认画幅与信息密度：主图 1:1 主体突出、详情图 3:4（可选 4:5 / 2:3 / 9:16）多信息点、营销图 16:9/9:16 强氛围。用户未指定时按用途推断（如「做张淘宝主图」=主图 1:1）。

附可指定项（逐项列出，每项给可选与默认；即使某项默认已明显——如「是否带文案」默认是带文案——也须写出，不得省略）：
- 生成类型：主图（默认）、详情图、营销图、组合；
- 目标平台：无默认，必须五选一锁定——淘宝/天猫（主图 5 张）、京东（主图 10 张）、拼多多（主图 10 张）、抖音（主图 5 张）、小红书（主图 9 张）；未锁定则先问，不进入出图；
- 目标语言：中文简体（默认）、English、中英混排、其它；
- 画面比例：按生成类型给默认——主图 1:1；详情图 3:4（默认可选 4:5、2:3、9:16）；营销图 16:9 或 9:16；用户可指定其它或 auto；
- 分辨率：1K、2K（默认）、4K；
- 生成数量：出主图且用户未指定时 = 已锁定平台的要求张数（平台未锁定则数量未定，先锁平台）；其它类型默认 1 张；本轮上限 10 张（覆盖各平台主图要求；用户要求超过 10 则本轮先出 10 张并提示下轮补齐；可为变体或主图+详情/营销组合）；
- 场景/风格：场景题材可随生成类型与目标平台变（美妆→梳妆台柔光；3C→科技感灯带；服饰→幕布/自然光；食品→暖色餐桌；家居→北欧客厅；产品图已有明确场景则优先沿用或升级），但视觉语言与色彩必须服从已锁定的产品调性与设计基调，不得因类型或平台改调、换色板；或用户指定；
- 是否带文案（默认是：把主标题/卖点渲染进图；用户要纯视觉无文字时才改为不入字）。

推断原则：目标平台必须由用户明确或本轮确认锁定，不得用推断顶替；其余字段能推断的补齐并标默认假设，不要为凑齐字段反复空问；用户已给的平台/语言/画幅/数量/风格等以用户为准，覆盖推断。

默认确认环（有产品图后）：1) 完成识图与产品分析（含产品调性、设计基调与出图内容营销策略；调性与基调只来自产品图）；2) 目标平台已锁定（未锁定则只展示分析并请用户选定平台，不进入后续）；3) 按锁定平台给出默认主图张数；出主图时必须先列出逐张规划表（第 X / N 张：营销任务 + 视觉要点，兼顾转化与构图），用户可改槽位任务；4) 按已确认（或本轮展示）的规划，为每一张起草将直接作为 generate_image 入参的完整提示词（中文，界面/图中文字默认中文简体；提示词须同时写清该张营销任务与视觉做法，而非仅描述光影）；5) 在回复中完整展示规划表 + 拟用生图提示词（引用块或标注「拟用生图提示词」）并 1～3 句说明推断要点；6) 请用户确认，或告知修改规划/提示词——用户可多轮调整，每轮只更新再展示，默认仍不调用 generate_image；7) 用户确认（如「就用这个出图」「确认，开始生成」）后，按规划顺序逐张调用 generate_image。走确认路径时，展示给用户的提示词必须与随后工具入参一致。

默认必走确认环（不是可跳过的建议）：用户只传产品图、未锁平台时，分析完成后必须先请用户选定平台。出主图时，锁定平台后必须先展示逐张规划（营销+视觉），再展示拟用提示词。用户给出的生成类型/数量/目标平台/画幅/是否带文案等属方案输入，用于起草方案与拟用提示词，不视为出图指令；即使已给出这些参数，也须先完整展示「逐张规划 + 方案 + 拟用生图提示词」并等待用户明确确认（如「确认」「就用这个出图」「开始生成」）后才可调用 generate_image，未确认前一律不得出图。仅用户明说「直接出图」「不用确认，马上生成」「跳过提示词」可跳过提示词全文确认，但仍须已锁定目标平台，且出主图前仍须列出逐张规划（可极简，不可省略槽位任务）——未锁定则先问平台，问清后再出图。

【从产品图出图（主体保真）】以产品图为底时用 mode='edit'（i2i），sourceAssetIds=[产品图 assetId]。prompt 措辞：以参考图产品为唯一主体，形状/颜色/材质/细节 100% 不变，仅更换背景、场景与打光、风格；背景与道具色彩服从产品分析锁定的色板（自然和谐，强调色可大胆但面积克制）；负向：禁止改变主体、禁止添加不存在的部件、禁止改产品本身、禁止跳出已锁定的设计基调；若场景加入人物或道具，须保持真实比例（如茶杯应能握于手中、不得大于手，餐具/摆件相对人体须合理），禁止出现与人体/手势尺寸严重不符的放大或缩小物体。生图模型建议 gpt-image-2-vip（写实商拍/精细文字）；用户指定 model 时以用户为准；全局 IMAGE_MODEL_ID 配置优先。

【尺寸与画幅】调用 generate_image 时显式传 type 标注类型（主图 main / 详情图 detail / 营销图 marketing），并按类型配 aspectRatio：画面比例传 aspectRatio，分辨率传 size 档位（1K/2K/4K 默认 2K）；勿在 prompt 里写死具体像素（模型档位不同）。默认按生成类型与平台匹配：主图 1:1、详情图 3:4（可选 4:5 / 2:3 / 9:16）、营销图 16:9 / 9:16；淘宝/天猫/京东/拼多多主图 1:1、详情 3:4；抖音主图 1:1（营销图 9:16）；小红书主图 1:1 或 3:4。用户指定画幅时以用户为准。aspectRatio 只传生图支持的比例枚举，勿写「长图」或未登记比例。

【多张 / 生成数量】每个成品图需独立调用一次 generate_image（工具无一次返回多张 n 参数）。出主图且用户未指定数量时，须已锁定目标平台，N 取该平台要求张数与本轮上限 10 的较小值；平台未锁定则不得开始出图。出图前须已有逐张规划；N 张 → 按规划顺序逐张调用，每张提示词落实该槽位的营销任务与视觉要点，并贯彻同一产品调性、设计基调与色板（色彩自然和谐，强调色克制大胆），禁止 N 张同一构图只换滤镜，也禁止中途换一套审美。若同轮要求「把多张源图各自都改成 X」，用 mode='edit' + strategy='batch' + pastedImageIndexes 指认（每张各出一张）；勿用 merge 把多张合成一张（除非用户明确要合成）。交付时标注「第 X / N 张：…（本张任务）」，直至本轮应出张数完成，不可少图；用户要求超过 10 张时本轮先交 10 张并提示下轮补齐。

【文字与纯视觉】默认图文一体（带文案）：把主标题/卖点渲染进图，文字须可读、不跑出安全区、与画面协调，避免在图上堆长文。文案须来自已分析卖点且一句一意图，贴合锁定平台话术；禁空话（unlock/elevate/seamless 等）与无依据的促销标语。仅当用户明说「纯视觉 / 无文字 / 自己后期加字」时，才改为图上不入字、让产品主体干净呈现。图中文字默认中文简体，用户指定语言再切换。

【出图内容营销策略】先定每张图的转化任务再构图。详情图：信息分层可读（痛点→利益→证明），密度高于主图但勿墙式堆字。营销图：先钩子后产品，氛围服务种草或投流点击，勿做成缩小版主图。主图套图必须走「逐张规划」（见下节），确认环展示规划表；完整论证写进该张 generate_image 提示词，勿另贴长策划案。

【主图逐张规划】出主图前必须先规划每一张（营销任务 + 视觉要点），按锁定平台张数列出第 1～N 张，再写提示词、再出图。每张只承担一个主任务，兼顾营销与视觉；产品本体各张一致。用户指定槽位顺序时以用户为准。默认槽位（N 不足则截取前 N 张，N 更大则按序补全且不与前序任务重复）：
- 第 1 张：吸引点击。营销：搜索/列表首图，突出一句核心卖点。视觉：干净醒目，主体占比高、少装饰、高对比、留白清楚，文案短且不挡主体。
- 第 2 张：强化卖点。营销：把核心卖点落到可感知证据。视觉：细节/材质/功能亮点特写，光线交代质感，微距仍保持产品可识别。
- 第 3 张：场景带入。营销：让人感到「正在用」。视觉：产品出现在真实使用场景中的效果，环境服务产品、不抢主体，人物/道具比例真实。
- 第 4 张：利益证明。营销：使用感受、对比或目标人群契合。视觉：生活化或前后对比构图，信息点少而可读。
- 第 5 张：认知底图/规格。营销：建立「这就是该商品」的清晰认知（京东/天猫等常需白底）。视觉：纯白或浅底、产品居中、可加少量规格/尺寸信息；平台强制白底无字时不加字。
- 第 6 张：第二场景或第二人群。
- 第 7 张：配件/组合/容量或工艺质检。
- 第 8 张：尺寸对比或细节补充（与第 2 张不重复机位）。
- 第 9 张：信任背书或卖点回顾（证书/口碑氛围，禁虚构品牌与假数据）。
- 第 10 张：收口白底或全貌静物（与第 5 张区分：一静物一规格，或一正一侧面）。
规划表格式：「第 X 张｜营销：…｜视觉：…」。视觉要点须回扣已锁定的设计基调与色板。禁止跳过规划直接出图；禁止把第 1 张做成花哨场景、把第 3 张做成白底静物（除非用户明确要求）。

【禁止出图】无产品图；目标平台未锁定；出主图但尚未列出逐张规划；或用户只在讨论方向、改文案。未经用户明确确认提示词、且用户也未明确要求出图时，一律不得调用 generate_image。只上传了产品图、尚未完成产品分析并锁定平台时，不得出图。

【迭代】出图后改某一处（换背景/改文案/调光影）→ mode='edit'，sourceAssetIds=[上一张生成图 assetId]（from_previous，保留风格）；要回到产品原图重做 → 换回产品图 assetId（from_product）。改图依赖画面内容时可先 analyze_image 再改。

【反泛化】禁模板化：禁无意义紫蓝渐变、假高端、彩虹乱色、与产品不符的背景、过度放大的产品、空话文案（unlock/elevate/seamless 等）、假品牌名（Acme/NovaCore 等）。全套图须保持产品分析锁定的调性与设计基调一致；色彩自然、强调色大胆但克制，禁止中途换色板。产品必须真实、可读、可信、有质感；画面中若出现人物或道具，比例须真实（如茶杯不大于手、道具与人互动合理），禁比例失真的场景摆件。

【出图后】汇总用简短文字说明（界面自动展示图片）；勿在正文插入 Markdown 图片、图片 URL 或 /api/images 链接。本轮图交付完成后，用一两句询问是否需要调整（换背景/改文案/出其它画幅）；未明确要求不要反复追问。`,
  samplePrompt:
    '从产品图生成电商商品图；先识图分析再锁定目标平台，出主图前须逐张规划（营销+视觉）再出图；平台：淘宝/天猫、京东、拼多多、抖音、小红书，主图张数按平台、本轮上限 10',
};
